#include<bits/stdc++.h>
using namespace std;
class Node{
    public:
    Node* prev;
    int val;
    Node* next;
    Node(int val){
        this->prev =NULL;
        this->val = val;
        this->next = NULL;
    }
};

class DLL_Stack{
    public:
    Node* head = NULL;
    Node* tail = NULL;
    int s =0;
    
    void push(int val){
        Node* newnode = new Node(val);
        s++;

        if (head == NULL)
        {
            head = newnode;
            tail = newnode;
            return;
        }
        tail->next = newnode;
        newnode->prev = tail;
        tail = newnode; 
    }

    int top(){
        if(tail ==NULL){
            return -1;
        }
        return tail->val;
    }

    void pop(){
        s--;
        if(head == NULL){
            return;
        }
        if(tail->prev == NULL){
            head=NULL;
            tail =NULL;
            return ;
        }
        tail = tail->prev;
        tail->next =NULL;
    }
    int size(){
        return s;
    }
    bool empty(){
        return head == NULL;
    }

};

int main(){
   DLL_Stack st;

   int n;cin>>n;
   int i =0;
   while (i<n)
   {
        int val;cin>>val;
        st.push(val);
        i++;
   }
   while (!st.empty())
   {
       cout<<st.top()<<endl;
       st.pop();
   }

}

Summary of the Code:

Implements a Stack using a Doubly Linked List (DLL) in C++.

Node class stores:

prev pointer
val (data)
next pointer

DLL_Stack class maintains:

head → bottom of the stack
tail → top of the stack
s → current size of the stack
Main Stack Operations:

push(val) → Inserts a new element at the top (tail) of the stack.
top() → Returns the value at the top of the stack.
pop() → Removes the top element from the stack.
size() → Returns the number of elements.
empty() → Checks if the stack is empty.

Program Flow in main()
Reads n (number of elements).
Pushes n values into the stack.
Pops and prints elements one by one, producing LIFO (Last In, First Out) order.
Key Idea
The stack behavior is achieved by inserting and removing nodes only from the tail of the doubly linked list, ensuring efficient O(1) push and pop operations.
